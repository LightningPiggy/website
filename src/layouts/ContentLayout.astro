---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description?: string;
  heroImage?: string;
  imageAlt?: string;
}

const { title, description, heroImage, imageAlt } = Astro.props;
// When a hero image is set, default the alt to the page title
const ogImageAlt = imageAlt || (heroImage ? title : undefined);
---

<BaseLayout title={title} description={description} image={heroImage} imageAlt={ogImageAlt}>
  <article class="py-8 md:py-12">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      {heroImage && (
        <div class="mb-8 -mx-4 sm:mx-0">
          <img
            src={heroImage}
            alt={title}
            class="w-full rounded-none sm:rounded-2xl object-cover max-h-96"
          />
        </div>
      )}

      <header class="mb-8">
        <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">{title}</h1>
        {description && (
          <p class="text-lg text-gray-600">{description}</p>
        )}
      </header>

      <div class="prose max-w-none">
        <slot />
      </div>
    </div>
  </article>
</BaseLayout>
